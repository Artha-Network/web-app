# Web app environment example

# Shared config (consumed by web & actions server deployments)
NEXT_PUBLIC_SOLANA_CLUSTER=devnet
NEXT_PUBLIC_USDC_MINT=usdc-mint-address
NEXT_PUBLIC_PROGRAM_ID=program-id
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=anon-key

# Vite-specific fallbacks
VITE_SOLANA_RPC=https://api.devnet.solana.com
VITE_SOLANA_CLUSTER=${NEXT_PUBLIC_SOLANA_CLUSTER}
VITE_SUPABASE_URL=${SUPABASE_URL}
VITE_SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}
VITE_PROGRAM_ID=${NEXT_PUBLIC_PROGRAM_ID}
VITE_USDC_MINT=${NEXT_PUBLIC_USDC_MINT}
